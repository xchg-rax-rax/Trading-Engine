load("@rules_foreign_cc//foreign_cc:defs.bzl", "cmake")

cmake(
    name = "crow",
    cache_entries = {
        "CMAKE_C_FLAGS": "-DCROW_BUILD_EXAMPLES=OFF -DCROW_BUILD_TESTS=OFF",
    },
    lib_source = "@crow//:all_srcs",
    out_static_libs = ["libcrow.a"],
)
